<div class="mx-auto max-w-3xl" id="wizard" data-suggested-port="{{suggestedPort}}">
  {{> page-header heading="CREATE SERVER" sub="One click to a running server in Simple mode — every option the image supports in Advanced."}}

  <div class="mb-4 flex flex-wrap items-center gap-2">
    <div class="seg" id="wz-source-tabs" role="tablist" aria-label="Server source">
      <button class="seg-btn" type="button" role="tab" aria-selected="true" data-source="vanilla">Vanilla</button>
      <button class="seg-btn" type="button" role="tab" aria-selected="false" data-source="mods">From mods</button>
      <button class="seg-btn" type="button" role="tab" aria-selected="false" data-source="modpack">From modpack</button>
      <button class="seg-btn" type="button" role="tab" aria-selected="false" data-source="blueprint">From blueprint</button>
    </div>
    <label class="ml-auto flex cursor-pointer items-center gap-2.5 text-sm font-medium" id="wz-detail-toggle">
      Advanced options
      <span class="msm-toggle"><input type="checkbox" id="wz-advanced-switch"><span></span></span>
    </label>
  </div>

  {{! "From mods" source tab — the server's creation hub. Two sub-modes:
      Choose loader (loader-first browser, default) and Auto-detect (the solver). }}
  <div class="mb-4 hidden space-y-3" id="wz-mods-panel">
    <div class="flex flex-wrap items-center gap-2">
      <div class="seg" id="wz-mods-mode" role="tablist" aria-label="Mod selection mode">
        <button class="seg-btn" type="button" role="tab" aria-selected="true" data-mode="browse">Choose loader &amp; mods</button>
        <button class="seg-btn" type="button" role="tab" aria-selected="false" data-mode="auto">Auto-detect from mods</button>
      </div>
    </div>

    {{! ---- Choose-loader browser (default) ---- }}
    <div class="card p-5" id="wz-mods-browse">
      <h3 class="mb-1 font-semibold">Build a modded server</h3>
      <p class="help mb-4">Pick a loader and version, then search {{#if curseforgeEnabled}}Modrinth &amp; CurseForge{{else}}Modrinth{{/if}} for mods — required dependencies are added automatically for you to review.</p>

      <label class="label">Mod loader</label>
      {{! .tile carries hover + focus ring + aria-pressed selection for every
          state — the initially-selected tile used to lose its hover affordance
          after deselection, and keyboard focus was invisible on all of them. }}
      <div class="mb-4 grid grid-cols-2 gap-2 sm:grid-cols-4" id="wz-loaders">
        <button type="button" data-loader="fabric" class="tile p-3" aria-pressed="true">{{{icon 'layers' 'size-5'}}}<span class="text-xs font-semibold">Fabric</span></button>
        <button type="button" data-loader="forge" class="tile p-3" aria-pressed="false">{{{icon 'hammer' 'size-5'}}}<span class="text-xs font-semibold">Forge</span></button>
        <button type="button" data-loader="neoforge" class="tile p-3" aria-pressed="false">{{{icon 'flame' 'size-5'}}}<span class="text-xs font-semibold">NeoForge</span></button>
        <button type="button" data-loader="quilt" class="tile p-3" aria-pressed="false">{{{icon 'grid-2x2' 'size-5'}}}<span class="text-xs font-semibold">Quilt</span></button>
      </div>

      <div class="grid gap-4 sm:grid-cols-2">
        <div>
          <label class="label" for="wz-mods-mc">Minecraft version</label>
          <select class="input" id="wz-mods-mc" data-label="Minecraft version"></select>
        </div>
        <div>
          <label class="label" for="wz-mods-loaderver">Loader build</label>
          <select class="input" id="wz-mods-loaderver" data-label="Loader build"><option value="">Latest (recommended)</option></select>
          <p class="help">Pin a specific loader build, or track the latest.</p>
        </div>
      </div>

      <div class="mt-4">
        <div class="flex flex-wrap items-center gap-2">
          <input class="input min-w-48 flex-1" id="wz-mods-q" placeholder="Search mods… e.g. sodium, create, jei" autocomplete="off">
          {{#if curseforgeEnabled}}
            <div class="seg" id="wz-mods-platforms" role="group" aria-label="Search platform">
              <button type="button" class="seg-btn" aria-pressed="true" data-platform="modrinth">Modrinth</button>
              <button type="button" class="seg-btn" aria-pressed="false" data-platform="curseforge">CurseForge</button>
            </div>
          {{/if}}
        </div>
        <div class="mt-2 hidden max-h-80 overflow-y-auto rounded-md border border-line" id="wz-mods-results"></div>
      </div>

      <div class="mt-4 hidden" id="wz-mods-selected-wrap">
        <div class="mb-2 flex items-center gap-2">
          <span class="text-sm font-semibold">Selected mods</span>
          <span class="badge" id="wz-mods-count">0</span>
          <span class="ml-auto text-xs text-ink-faint" id="wz-mods-dephint"></span>
        </div>
        <div class="space-y-2" id="wz-mods-selected"></div>
      </div>
    </div>

    {{! ---- Auto-detect (the compatibility solver) ---- }}
    <div class="card p-5 hidden" id="wz-solver">
      <h3 class="mb-1 font-semibold">Auto-detect loader &amp; version</h3>
      <p class="help mb-4">Search {{#if curseforgeEnabled}}Modrinth or CurseForge{{else}}Modrinth{{/if}}, add the mods you want, and Minecraft Server Manager finds the newest loader + Minecraft version that every one of them supports.</p>
      <div>
        <label class="label" for="wz-solver-q">Search mods</label>
        <div class="flex flex-wrap items-center gap-2">
          <input class="input min-w-48 flex-1" id="wz-solver-q" placeholder="e.g. sodium, create, jei…" autocomplete="off">
          {{#if curseforgeEnabled}}
            <div class="seg" id="wz-solver-platforms" role="group" aria-label="Search platform">
              <button type="button" class="seg-btn" aria-pressed="true" data-platform="modrinth">Modrinth</button>
              <button type="button" class="seg-btn" aria-pressed="false" data-platform="curseforge">CurseForge</button>
            </div>
          {{/if}}
        </div>
      </div>
      <div class="mt-2 hidden max-h-72 overflow-y-auto rounded-md border border-line" id="wz-solver-results"></div>
      <div class="mt-3 hidden flex-wrap gap-1.5" id="wz-solver-chips"></div>
      <div class="mt-4 flex items-center gap-3">
        <button class="btn btn-primary" id="wz-solver-run" type="button" disabled>{{{icon 'wand-sparkles' 'size-4'}}} Solve compatibility</button>
        <span class="text-xs text-ink-faint" id="wz-solver-hint">Add at least one mod to solve.</span>
      </div>
      <div class="mt-4 hidden" id="wz-solver-result"></div>
      <input type="hidden" id="wz-solver-mods" value="">
    </div>
  </div>

  {{! U1/U2: "From modpack" source tab — search, pin a version, real one-task install }}
  <div class="card mb-4 hidden p-5" id="wz-modpack">
    <h3 class="mb-1 font-semibold">Pick a modpack</h3>
    <p class="help mb-4">Search Modrinth or CurseForge. The pack decides the loader and Minecraft version, and the install is always pinned to the exact pack version you choose — restarts never silently upgrade.</p>
    {{! GTNH ships outside Modrinth/CurseForge — a fixed card, not a search result }}
    <div class="mb-4 flex flex-wrap items-center gap-3 rounded-md border border-line p-3" id="wz-gtnh-card">
      <div class="min-w-0 flex-1">
        <div class="font-semibold">GT New Horizons</div>
        <div class="text-xs text-ink-faint">Minecraft 1.7.10 expert pack — needs 6 GB+ RAM and 20 GB+ disk. Runs on modern
          Java.</div>
      </div>
      <button type="button" class="btn shrink-0" id="wz-gtnh-pick">Set up</button>
    </div>
    {{! Custom zip — CurseForge modpack export or a hand-assembled zip of jars }}
    <div class="mb-4 flex flex-wrap items-center gap-3 rounded-md border border-line p-3" id="wz-zip-card">
      <div class="min-w-0 flex-1">
        <div class="font-semibold">Custom zip</div>
        <div class="text-xs text-ink-faint">A CurseForge modpack export (with manifest.json{{#unless curseforgeEnabled}} — needs the API key in Settings{{/unless}}) or any zip of mod jars you collected yourself. Every jar is identified and installed with provenance.</div>
      </div>
      <button type="button" class="btn shrink-0" id="wz-zip-pick">Upload zip</button>
    </div>
    <div class="mb-4 hidden" id="wz-zip-selected"></div>
    <div class="flex flex-wrap items-center gap-2">
      <input class="input min-w-48 flex-1" id="wz-pack-q" placeholder="e.g. Simply Optimized, All the Mods…" autocomplete="off">
      <div class="seg" id="wz-pack-platforms" role="group" aria-label="Search platform">
        <button type="button" class="seg-btn" aria-pressed="true" data-platform="modrinth">Modrinth</button>
        <button type="button" class="seg-btn" aria-pressed="false" data-platform="curseforge">CurseForge</button>
      </div>
    </div>
    <div class="mt-2 hidden max-h-80 overflow-y-auto rounded-md border border-line" id="wz-pack-results"></div>
    <div class="mt-4 hidden" id="wz-pack-selected">
      <div class="flex items-center gap-3 rounded-md border border-grass-700 bg-grass-600/10 p-3" id="wz-pack-summary"></div>
      <div class="mt-3 grid items-end gap-4 sm:grid-cols-[1fr_auto]">
        <div>
          <label class="label" for="wz-pack-version">Pack version
            <span class="text-xs font-normal text-ink-faint">(always pinned)</span></label>
          <select class="input" id="wz-pack-version" data-label="Pack version"></select>
          <label class="mt-2 hidden items-center gap-2 text-xs text-ink-faint" id="wz-gtnh-beta-row">
            <input type="checkbox" id="wz-gtnh-beta" />
            include beta releases
          </label>
        </div>
        <button type="button" class="btn mb-0.5" id="wz-pack-details-btn">{{{icon 'book-open' 'size-4'}}} View details</button>
      </div>
    </div>
  </div>

  {{! "From blueprint" source tab — identity stays editable, the blueprint owns the rest }}
  <div class="card mb-4 hidden p-5" id="wz-blueprint-panel">
    <h3 class="mb-1 font-semibold">Start from a blueprint</h3>
    <p class="help mb-4">A blueprint sets the server type, Minecraft version, modpack, mods, config and resources — you choose the name, icon and color below. The sections a blueprint owns are greyed out.</p>
    <label class="label" for="wz-blueprint">Blueprint</label>
    <select class="input" id="wz-blueprint" data-label="Blueprint">
      <option value="">Choose a blueprint…</option>
      {{#each blueprints}}<option value="{{id}}" data-desc="{{notes}}">{{name}}</option>{{/each}}
    </select>
    {{#unless blueprints.length}}
      <p class="help mt-2">No blueprints in the library yet — export one from a server's Settings tab, or import a .mcserver.zip on the <a href="/blueprints" class="text-link hover:underline">Blueprints page</a>.</p>
    {{/unless}}
  </div>

  {{! Cards are unnumbered on purpose: tabs hide the type card, so hard-coded
      "1…4" used to count 1 → 3 → 4. }}
  <div class="card p-5">
    <h3 class="mb-4 font-semibold">Identity</h3>
    <div class="grid gap-4 sm:grid-cols-[minmax(14rem,1fr)_auto]">
      <div>
        <label class="label" for="wz-name">Server name</label>
        <input class="input" id="wz-name" placeholder="e.g. Weekend Survival" autocomplete="off">
      </div>
      <div>
        <label class="label">Icon & color</label>
        {{! flex-wrap: the icon row must give way instead of crushing the name input at ~700px. }}
        <div class="flex flex-wrap gap-1.5" id="wz-icons">
          <button type="button" data-icon="grass" class="tile size-10 p-1" aria-pressed="true"><img src="/icons/servers/grass.png" alt="Grass" class="size-full rounded-sm"></button>
          <button type="button" data-icon="creeper" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/creeper.png" alt="Creeper" class="size-full rounded-sm"></button>
          <button type="button" data-icon="diamond" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/diamond.png" alt="Diamond" class="size-full rounded-sm"></button>
          <button type="button" data-icon="tnt" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/tnt.png" alt="TNT" class="size-full rounded-sm"></button>
          <button type="button" data-icon="portal" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/portal.png" alt="Portal" class="size-full rounded-sm"></button>
          <button type="button" data-icon="chest" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/chest.png" alt="Chest" class="size-full rounded-sm"></button>
          <button type="button" data-icon="sword" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/sword.png" alt="Sword" class="size-full rounded-sm"></button>
          <button type="button" data-icon="potion" class="tile size-10 p-1" aria-pressed="false"><img src="/icons/servers/potion.png" alt="Potion" class="size-full rounded-sm"></button>
        </div>
      </div>
    </div>
    <div class="mt-3 grid gap-4 sm:grid-cols-[minmax(14rem,1fr)_auto]">
      <div>
        <label class="label" for="wz-desc">Description <span class="text-xs font-normal text-ink-faint">(optional)</span></label>
        <input class="input" id="wz-desc" placeholder="What is this server for?">
      </div>
      <div>
        <label class="label">Accent</label>
        {{! .swatch: theme-aware selected ring — the old border-white/60 marker
            was invisible on the light theme's white card. }}
        <div class="flex flex-wrap gap-1.5" id="wz-colors">
          <button type="button" data-accent="#3fa62b" class="swatch size-10" style="background:#3fa62b" data-tip="Grass" aria-pressed="true"></button>
          <button type="button" data-accent="#21a7ab" class="swatch size-10" style="background:#21a7ab" data-tip="Diamond" aria-pressed="false"></button>
          <button type="button" data-accent="#e5484d" class="swatch size-10" style="background:#e5484d" data-tip="Redstone" aria-pressed="false"></button>
          <button type="button" data-accent="#e99417" class="swatch size-10" style="background:#e99417" data-tip="Gold" aria-pressed="false"></button>
          {{! Amethyst — the palette's purple, from the in-game block }}
          <button type="button" data-accent="#9a5cc6" class="swatch size-10" style="background:#9a5cc6" data-tip="Amethyst" aria-pressed="false"></button>
        </div>
      </div>
    </div>
  </div>

  <div class="card mt-4 p-5" id="wz-card-flavor">
    <h3 class="mb-4 font-semibold">Server type &amp; version</h3>
    <p class="help mb-3">Vanilla and plugin servers. Want mods? Use the <b>From mods</b> tab — that's where the mod loaders (Fabric, Forge, NeoForge, Quilt) live.</p>
    <div class="mb-4 grid grid-cols-3 gap-2" id="wz-flavors">
      <button type="button" data-type="VANILLA" class="tile p-3" aria-pressed="true">{{{icon 'box' 'size-5'}}}<span class="text-xs font-semibold">Vanilla</span></button>
      <button type="button" data-type="PAPER" class="tile p-3" aria-pressed="false">{{{icon 'scroll-text' 'size-5'}}}<span class="text-xs font-semibold">Paper</span></button>
      <button type="button" data-type="PURPUR" class="tile p-3" aria-pressed="false">{{{icon 'gem' 'size-5'}}}<span class="text-xs font-semibold">Purpur</span></button>
    </div>
    <div class="grid gap-4 sm:grid-cols-2">
      <div>
        <label class="label" for="wz-version">Minecraft version</label>
        <select class="input" id="wz-version" data-label="Minecraft version">
          <option value="LATEST" selected>LATEST ({{latestRelease}})</option>
          {{#each versions}}<option value="{{id}}"{{#unless (eq type "release")}} data-desc="{{#if (eq type "snapshot")}}Snapshot{{else if (eq type "old_beta")}}Beta{{else}}Alpha{{/if}}"{{/unless}}>{{id}}</option>{{/each}}
        </select>
        <p class="help">Every Mojang version — releases, snapshots, betas and alphas. The matching Java image is picked automatically.</p>
      </div>
      <div>
        <label class="label" for="wz-port">Game port</label>
        <input class="input max-w-48 font-mono" id="wz-port" value="{{suggestedPort}}" inputmode="numeric">
        <p class="help" id="wz-port-help">Auto-suggested free port. RCON gets port + 1000.</p>
      </div>
    </div>
  </div>

  <div class="card mt-4 p-5" id="wz-card-world">
    <h3 class="mb-4 font-semibold">World &amp; rules</h3>
    <div class="grid gap-4 sm:grid-cols-2">
      <div>
        <label class="label" for="wz-seed">World seed <span class="text-xs font-normal text-ink-faint">(optional)</span></label>
        <input class="input font-mono" id="wz-seed" placeholder="random">
      </div>
      <div>
        <label class="label" for="wz-maxplayers">Max players</label>
        <input class="input max-w-48" id="wz-maxplayers" type="number" inputmode="numeric" value="10" min="1" max="1000">
      </div>
      <div>
        <label class="label" for="wz-difficulty">Difficulty</label>
        <select class="input" id="wz-difficulty" data-label="Difficulty">
          <option value="peaceful">Peaceful</option><option value="easy">Easy</option><option value="normal" selected>Normal</option><option value="hard">Hard</option>
        </select>
      </div>
      <div>
        <label class="label" for="wz-mode">Game mode</label>
        <select class="input" id="wz-mode" data-label="Game mode">
          <option value="survival" selected>Survival</option><option value="creative">Creative</option><option value="adventure">Adventure</option><option value="spectator">Spectator</option>
        </select>
      </div>
      <div>
        <label class="label" for="wz-pvp">PvP</label>
        <select class="input" id="wz-pvp" data-label="PvP">
          <option value="true" selected>On</option><option value="false">Off</option>
        </select>
        <p class="help">Applied from the first start — no restart needed.</p>
      </div>
    </div>
    <div class="mt-4">
      <label class="label" for="wz-motd">MOTD <span class="text-xs font-normal text-ink-faint">— &amp; color codes supported</span></label>
      <input class="input font-mono" id="wz-motd" value="&aA Minecraft server">
      <div class="console mt-2 py-2 text-sm" id="wz-motd-preview"></div>
    </div>
  </div>

  <div class="card mt-4 p-5" id="wz-card-resources">
    <h3 class="mb-4 font-semibold">Resources</h3>
    <div>
      <label class="label">RAM (Java heap)</label>
      <div class="flex items-center gap-3">
        <input type="range" min="1024" max="16384" step="512" value="4096" class="msm-range flex-1" id="wz-ram" data-out="wz-ram-out" data-unit=" MB">
        <span class="w-24 text-right font-mono text-sm" id="wz-ram-out">4096 MB</span>
      </div>
      <p class="help">The container's hard limit is set automatically with 50% headroom.</p>
    </div>
    <div class="mt-4">
      <label class="label">Disk quota</label>
      <div class="flex items-center gap-3">
        <input type="range" min="0" max="200" step="5" value="25" class="msm-range flex-1" id="wz-quota" data-out="wz-quota-out" data-unit=" GB" data-zero="off">
        <span class="w-24 text-right font-mono text-sm" id="wz-quota-out">25 GB</span>
      </div>
    </div>
  </div>

  <div id="wz-advanced" class="mt-4 hidden space-y-3">
    <div class="rounded-md border border-link/40 bg-diamond-500/10 p-3 text-xs text-ink-soft">
      {{{icon 'info' 'size-3.5'}}} Every environment variable the Docker image supports, straight from its docs. Leave a field empty to use the image default —
      only values you change are applied. Simple-mode picks above (version, port, world rules, resources) still apply.
    </div>
    {{#each advancedSections}}
      <details class="card group">
        <summary class="flex cursor-pointer items-center gap-2.5 p-4 font-semibold marker:content-none">
          {{{icon icon 'size-4 text-ink-faint'}}} {{label}}
          <span class="badge ml-1">{{fields.length}}</span>
          <span class="ml-auto text-ink-faint transition group-open:rotate-180">{{{icon 'chevron-down' 'size-4'}}}</span>
        </summary>
        <div class="divide-y divide-line/60 border-t border-line px-4 pb-4">
          {{#each fields}}
            {{> catalog-field}}
          {{/each}}
        </div>
      </details>
    {{/each}}
    <details class="card group">
      <summary class="flex cursor-pointer items-center gap-2.5 p-4 font-semibold marker:content-none">
        {{{icon 'terminal' 'size-4 text-ink-faint'}}} Extra environment variables
        <span class="ml-auto text-ink-faint transition group-open:rotate-180">{{{icon 'chevron-down' 'size-4'}}}</span>
      </summary>
      <div class="border-t border-line p-4">
        <textarea class="input font-mono text-xs" id="wz-extra-env" rows="4" placeholder="KEY=value&#10;ANOTHER_KEY=value"></textarea>
        <p class="help">Raw escape hatch for anything new the image adds before the catalog catches up. One KEY=value per line; these override everything above.</p>
      </div>
    </details>
    {{! Admin-only: extra binds mount arbitrary host paths (root-equivalent
        through the Docker socket) — the API rejects these fields for other
        roles, so don't render dead controls for them. }}
    {{#if (eq user.role 'admin')}}
    <details class="card group">
      <summary class="flex cursor-pointer items-center gap-2.5 p-4 font-semibold marker:content-none">
        {{{icon 'container' 'size-4 text-ink-faint'}}} Advanced Docker settings
        <span class="ml-auto text-ink-faint transition group-open:rotate-180">{{{icon 'chevron-down' 'size-4'}}}</span>
      </summary>
      <div class="space-y-4 border-t border-line p-4">
        <div class="grid gap-4 sm:grid-cols-2">
          <div>
            <label class="label" for="wz-docker-name">Container name <span class="text-xs font-normal text-ink-faint">(optional)</span></label>
            <input class="input font-mono" id="wz-docker-name" placeholder="msm-<auto>" autocomplete="off">
            <p class="help">Letters, digits, "_", ".", "-". Leave blank for the default.</p>
          </div>
          <div>
            <label class="label" for="wz-docker-network">Docker network <span class="text-xs font-normal text-ink-faint">(optional)</span></label>
            <select class="input" id="wz-docker-network" data-label="Docker network">
              <option value="">Default (bridge)</option>
            </select>
            <p class="help">Attach to an existing network — handy for a reverse proxy like Pangolin or NGINX.</p>
          </div>
        </div>

        <div>
          <div class="mb-2 flex items-center justify-between">
            <span class="text-sm font-semibold">Extra port mappings</span>
            <button type="button" class="btn btn-sm" id="wz-docker-port-add">{{{icon 'plus' 'size-3.5'}}} Add port</button>
          </div>
          <div class="space-y-2" id="wz-docker-ports"></div>
          <p class="help">e.g. UDP 19132 for Bedrock/Geyser, TCP 8100 for BlueMap.</p>
        </div>

        <div>
          <div class="mb-2 flex items-center justify-between">
            <span class="text-sm font-semibold">Extra volume binds</span>
            <button type="button" class="btn btn-sm" id="wz-docker-bind-add">{{{icon 'plus' 'size-3.5'}}} Add bind</button>
          </div>
          <div class="space-y-2" id="wz-docker-binds"></div>
          <p class="help">Free-form host paths — the panel already has Docker-socket (root-equivalent) access, so double-check these before creating.</p>
        </div>

        <div class="flex justify-end">
          <button type="button" class="btn" id="wz-docker-preview">{{{icon 'file-code' 'size-4'}}} Preview as YAML</button>
        </div>
      </div>
    </details>
    {{/if}}
  </div>

  <div class="mt-6 flex items-center justify-between">
    <a href="/" class="btn btn-ghost">Cancel</a>
    <button class="btn btn-primary" id="wz-create" type="button">{{{icon 'pickaxe' 'size-4'}}} Create &amp; start</button>
  </div>
</div>
<script type="module" src="/js/pages/wizard.js"></script>
